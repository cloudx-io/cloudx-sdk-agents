---
applies_to: [flutter]
signals: ["always load for Flutter projects", "pubspec.yaml"]
last_verified: 2026-08-03
status: stub
---

# Flutter specifics

> Stub — to be seeded from real support cases. The Flutter integration docs
> page is the source of truth for package installation and API usage
> (`en/flutter/integration.md`).

Flutter-specific knowledge to capture here as cases surface:

- Platform-channel init timing relative to `runApp`.
- Android embedding and iOS Podfile requirements the plugin assumes.
- Hot-reload / hot-restart behavior with live ad objects.
- Widget-tree integration patterns for banner placement.
